let x: int = 10;
let y: string = "hello";
let z = x + 5; // inferred float (my checker returns float for +)

fn add(a: int, b: int) -> int {
    return a + b;
}

print("x: ", x);
print("y: ", y);
print("add(5, 3): ", add(5, 3));

// This should work (gradual typing)
let mismatched: int = "should be a warning if I uncomment the warning logic";
print("mismatched: ", mismatched);
